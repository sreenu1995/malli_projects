package com.employee.mapper;

import org.mapstruct.Mapper;

@Mapper
public abstract class EmployeeMapper {

	/*
	 * Employee toEmployee(EmployeeDto employeeDto); EmployeeDto
	 * toEmployeeDto(Employee employee);
	 */
}
